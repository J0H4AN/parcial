<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Biblioteca"
        tools:targetApi="31">

        <!-- Actividad principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Biblioteca">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividades adicionales para las distintas pantallas -->
        <activity
            android:name=".Screen.AutorScreen"
            android:exported="true"
            android:label="Autores"
            android:theme="@style/Theme.Biblioteca" />

        <activity
            android:name=".Screen.LibroScreen"
            android:exported="true"
            android:label="Libros"
            android:theme="@style/Theme.Biblioteca" />

        <activity
            android:name=".Screen.MiembroScreen"
            android:exported="true"
            android:label="Miembros"
            android:theme="@style/Theme.Biblioteca" />

        <activity
            android:name=".Screen.PrestamoScreen"
            android:exported="true"
            android:label="Préstamos"
            android:theme="@style/Theme.Biblioteca" />

    </application>

</manifest>
